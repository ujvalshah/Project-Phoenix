# API: Search

Routes under `/api/search` via [`server/src/routes/search.ts`](../../server/src/routes/search.ts).

## Perf topics

Autocomplete / suggestion paths and duplicated collection expansion work.
