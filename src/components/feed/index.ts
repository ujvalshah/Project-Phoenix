/** Homepage grid virtualization (legacy `/feed` route components were removed; routes redirect to `/`). */

export { HomeGridVirtualized } from './HomeGridVirtualized';
export type { HomeGridVirtualizedApi } from './HomeGridVirtualized';
