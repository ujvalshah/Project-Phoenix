/**
 * Shared feed prefixes (no deps on {@link ./articleKeys} — safe for importing from cache helpers).
 */

/** Mirrors `feedKeys.collectionEntriesInfiniteAllPrefix` tuples. */
export const FEED_COLLECTION_ENTRIES_INFINITE_PREFIX = ['feed', 'collection-entries', 'infinite'] as const;
