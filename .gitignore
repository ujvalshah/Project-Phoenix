# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Lighthouse CLI drops (regenerated locally; do not commit binary-sized JSON)
lighthouse-*.json

node_modules
# Vite output at repo root only (`dist` without slash would also match `server/dist`)
/dist
/dist-ssr
# Compiled Express (`npm run build` / `npm run build:server`)
server/dist/
*.local

# Environment variables
.env
.env.local
.env.*.local
**/.env
**/server/.env
.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Temporary files and archives
*.zip
*_EXTRACT.txt
*_EXTRACT*.txt

.vercel
.env*.local

# Windows reserved names (avoid accidental creation)
nul
NUL
con
CON

# System files and directories
AppData/
OneDrive/
Contacts/
CrossDevice/
Documents/
Downloads/
Favorites/
Links/
Music/
Saved Games/
Searches/
Videos/
NTUSER.DAT*
ntuser.dat*
ntuser.ini
*.regtrans-ms
*.blf
*.LOG*

# Browser and system directories
Application Data/
Cookies/
Local Settings/
My Documents/
NetHood/
PrintHood/
Recent/
SendTo/
Start Menu/
Templates/

# IDE and tool directories
.antigravity/
.claude/
.claude.json*
.codeium/
.config/
.cursor/*
!.cursor/rules/
!.cursor/rules/*.mdc
.gemini/
.lesshst
.local/
.ssh/
.windsurf/

# E2E test auth state
tests/e2e/.auth-state.json

# Test results and reports
test-results/
playwright-report/
reports/
scripts/validate/output/

# Local perf collector (npm run perf:collect)
output/perf-results.json
output/perf-results.csv
output/home-feed-append-perf-results.json

# Local tool / audit drops (regenerated; do not commit)
eslint-errors.json
lighthouse-report*.html
# Stray emit next to `nugget.ts` if tsc is run without project outDir
src/types/nugget.js
src/types/nugget.js.map
